# CHO Derivation Ledger

Frozen date: 2026-06-06

Purpose: make the logical status of each CHO claim explicit. This ledger separates theorem-level consequences, derived bridge claims, motivated ansaetze, and numerical comparisons so the project can progress by tightening the weakest links instead of adding more formulas.

Parameter convention: describe CHO as a **few-input** framework. The audit does not fit a separate continuous low-energy parameter for each row, but it does rely on explicit algebraic inputs, bridge assumptions, and continuum/RG matching prescriptions.

## Status Key

| Status | Meaning | What would strengthen it |
|---|---|---|
| Theorem | Mathematical result follows from stated algebraic assumptions | Clean citation/proof and precise physical identification |
| Derived bridge | Formula follows after one named physics-to-algebra bridge is accepted | Derive the bridge from the CHO action or representation theory |
| Open bridge | Numerically strong and structurally motivated, but the bridge is incomplete | Construct the operator, mass matrix, or continuum limit explicitly |
| Ansatz | Motivated formula currently chosen from available algebraic factors | Replace with a derivation or demote from headline count |
| Future test | Not yet experimentally settled | Keep the target fixed and compare against future data |

## Canonical Counting

The compute table currently contains 25 quantitative rows. The paper text often says 23 because some rows are grouped as sector relations rather than listed as individual masses. For external-facing material, use:

- Headline claim: 23 grouped relations, matching the former electroweak draft's grouping convention.
- Audit claim: 25 table rows in `compute/summary_table.py`, where `m_c`, `m_s`, and `m_mu` are displayed explicitly.

Do not mix these counts without explaining the grouping.

## Core Assumptions

| ID | Assumption | Current role | Open issue |
|---|---|---|---|
| A1 | Physics algebra `A = C x H x O` | Starting structure for internal degrees of freedom | Justify uniqueness as physics input, not only post-hoc fit. NOTE: the algebra choice and its SM gauge content are established prior work (Furey, Dixon, Todorov-Dubois-Violette, Boyle-Krasnov); cite, do not claim as novel |
| A2 | Fermions are minimal left ideals of `A` | Gives one-generation state space | Tighten map from ideal basis to SM representations. NOTE: the minimal-ideal one-generation construction is Furey's (and Dubois-Violette's) result, granted by peers; cite directly (see `COMPARISON.md`, Q1) |
| A3 | Triality sectors correspond to generations | Gives family structure | SUPERSEDED as the generation map: `three_generations_frame.py` shows generations are the three frame IDEMPOTENTS (points of OP^2=F4/Spin(9)) permuted by the INNER frame S3, not the outer-triality reps 8v/8s/8c. The remaining task is functorial: map the SM fermion content onto the tangent spinor Delta_9. The Yukawa SPECTRUM is now LOCALISED (not wholly open): `spectral_action_432.py` derives the parameter-free averaging law on `L_X` and reduces the generation hierarchy to one spurion scale `eps0`, leaving a single scalar seed function (the three diagonal eigenvalues' profile) as the lone open input; `generation_cascade.py` then collapses that seed further — because the generations are the three roots of the `J3(O)` Freudenthal cubic, the light-pair product equals the cubic norm over the heaviest mass (a derived seesaw) and the whole sector hierarchy reduces to TWO integer invariant-suppression orders `(q,Q)=(ord T2, ord N3)` (leptons `(1,4)`=triangular); `cascade_universality.py` then shows that `(q,Q)` sector-dependence is the already-derived flavour data, not new freedom — the universal seesaw skeleton is `(0,2,4)`, and dividing the middle exponent by the independently-derived Georgi-Jarlskog factors `{1,3,8}` gives a UNIVERSAL `2.00 +/- 0.01`, so the second-generation suppression is universally `eps0^2` and the only remaining non-universality is the already-open first-generation prefactors (lepton `1/(4 pi)`) |
| A4 | CHO information/lattice action sets scales | Source of base-3 suppressions and continuum parameters | Derive continuum limit and RG boundary conditions |
| A5 | Planck mass is the only dimensional input | Fixes all mass scales | Keep all low-energy inputs out of derivations except final comparison |
| A6 | Continuum/RG matching prescriptions | Connects algebraic-scale relations to measured low-energy constants | Phase 4 gate `compute/rg_matching_audit.py` now makes inverse-matched scales and threshold inputs explicit; still derive matching scales and thresholds from the action |

## Quantitative Ledger

| ID | Observable or claim | Formula or value | Status | Main proof obligation |
|---|---|---|---|---|
| G1 | Number of generations | `N_gen = 3` | COUNT + CHIRALITY now obstruction-free via the inner-frame route; SPECTRUM still open | `three_generations_nogo_audit.py` confirms the SOUND part (D4 triality = S3 gives exactly three inequivalent 8-dim reps 8v, 8s, 8c) and that identifying generations with those reps faces two Distler-Garibaldi-style obstructions (triality mixes vector 8v with spinors 8s/8c; 8s, 8c are opposite-chirality mirror partners). RESOLVED by `three_generations_frame.py`: generations are NOT the triality-permuted reps but the three primitive IDEMPOTENTS of a J3(O) frame -- three identical points of OP^2 = F4/Spin(9) (verified: dim Der(J3O)=52, isotropy 36=spin(9) and tangent 16 at all three idempotents), permuted by the INNER frame S3 < F4. Since F4 is connected with no outer automorphism, this S3 cannot map a rep to an inequivalent one (Obstruction 1 cannot be posed); and each generation's 16-dim tangent is ONE real Spin(9) spinor Delta_9 (octonionic Cl(9), commutant dimension 1 = real type) = C(x)O = Lever B's KO-dim-6 module, so three identical self-conjugate copies share one chirality with no mirror partner (Obstruction 2 cannot arise). The 8v/8s/8c the no-go attacked are the OFF-DIAGONAL Peirce slots, a different decomposition (PART D confirms they mix chirality). Lever A (`jordan_eigenvalue_generations.py`) independently gives the same count as the DEGREE of the J3(O) cubic norm (= rank = three orthogonal idempotents). OPEN: the fermion-content map onto T(OP^2), the Dirac operator D, and the Yukawa SPECTRUM (a flavour-diagonal element still returns only its seeded diagonal). NOTE: per-generation gauge quantum numbers are no longer open -- charge (Lever C), and now weak isospin + the H factor + the full hypercharge spectrum (Lever D, `weak_isospin_hypercharge.py`) are derived; only the chiral doublet/singlet projection (KO-dim-6) and the Yukawa magnitudes remain |
| G2 | No fourth generation | sedenion/J4(O) obstruction | Theorem, conditional on A2; count+chirality bridge now obstruction-free (see G1) | Clarify why any fourth family must require the obstructed extension; the A3 caveat inherited from G1 is now resolved at the count+chirality level via the inner-frame/idempotent route (`three_generations_frame.py`), spectrum still open |
| S1 | Electroweak hierarchy / W mass | `M_W = M_P / 3^36` | Derived bridge | Derive each `1/3` factor from the lattice action and fix normalization without using `v` |
| S2 | Top mass | `m_t = v / sqrt(2)` | Derived bridge | Prove unique top Yukawa saturation and compute threshold correction |
| S3 | Higgs mass | `m_H = v sqrt(pi/12)` | Derived bridge | Derive Higgs quartic normalization from D4/root geometry in the field theory action |
| S4 | Fine structure constant | `alpha^-1(0) = 128 pi / 3 + VP` | Open bridge / Phase 4 gate explicit / matching scale falsified as single number | `compute/rg_matching_audit.py` shows the CHO boundary `128 pi/3 = 134.041` needs `Delta alpha^-1 = +2.995`; a leptonic-only inverse scale is `5.054 GeV`, while the legacy `0.700 GeV` QCD-scale example still needs a hadronic VP remainder `1.061`. `compute/rg_scale_derivation.py` (Item 3) sharpens this to a falsification: CHO's TWO electroweak boundaries (`alpha_em^-1 = 128 pi/3` AND `sin^2 = 1/4`) fix BOTH gauge couplings `alpha_1^-1 = 96 pi/5 = 60.319` and `alpha_2^-1 = 32 pi/3 = 33.510` at one scale, but one-loop running reaches them at `mu ~ 12 GeV` vs `mu ~ 2.2e5 GeV` respectively — a `1.8e4` discrepancy, so NO single matching scale exists. Complete the lattice-to-continuum matching and vacuum-polarization calculation before promotion |
| S5 | Weinberg angle | `sin^2(theta_W) = 1/4 + RG` | Open bridge / Phase 4 gate explicit / matching scale falsified as single number | With standard one-loop SM running, `compute/rg_matching_audit.py` finds `sin^2(theta_W)=1/4` at `mu_* = 3.679e3 GeV` by inverse running; the seesaw scale gives `0.401745` and Planck gives `0.471320`. `compute/rg_scale_derivation.py` (Item 3) confirms the scale is inverse-fit, not derived: `mu_*` is `M_P/3^32.5` (a NON-integer power of 3, so not a clean CHO scale), no independently-derived CHO scale (`v`->`0.236`, `M_W`->`0.231`, seesaw->`0.402`, `M_P`->`0.471`) lands at `1/4`, and the two electroweak boundaries over-determine the scale by `1.8e4` (see S4). Fix the matching scale and thresholds from CHO, not by inverse running |
| F0 | Triality-breaking parameter | `epsilon_0^2 = pi/432` | Open bridge / action-selected `pi`; `pi`, `16`, `27` geometric, the two `16`s the same Spin(9) spinor, R1+R2 reframed (the two-level symmetry now traced to `A4`); the flat `1/16` and `1/27` weights now Schur-forced by Spin(9)- and `E6`-irreducibility; residuals are vacuum purity + one frame choice + the `A4` flavour-symmetry origin + deriving sector-independence (Assumption P, now reduced to the single clause "factor-wise `Spin(9) x E6` product-invariance" by `compute/epsilon_product_irreducible.py`, since `Delta_9 (x) J3(O)` is product-irreducible so by Schur ANY spurion — separable OR entangled — averages to `I_432/432`; `compute/epsilon_symplectic_volume.py` then reframes the PRODUCT as one geometric-quantization object — `16` and `27` are Bohr-Sommerfeld counts of single `Spin(9)`-spinor and `E6`-minimal coadjoint orbits, so `432` is the Liouville volume of one PRODUCT orbit and the factorization becomes an orbit-method theorem rather than an assumption, and `compute/epsilon_factor_forcedness.py` shows `16 x 27` is the UNIQUE factorization of `432` whose both factors are independently-derived carriers while `432` is the minimal rep of no single simple group, so a product is forced and the live seam narrows to which two orbits the action quantizes; `compute/epsilon_orbit_selection.py` then answers WHICH two -- the two MINIMAL (coherent-state) orbits, FORCED by `Spin(9)`-transitivity on the spinor sphere `S^15` for the `16` (the spinor orbit is unique: orbit-tangent dim `15` everywhere, stabiliser `21 = dim Spin(7)`) and by the action's own rank-one condition for the `27` (the `E6`-minimal orbit = the Freudenthal rank-one variety `X#=0`), interlocked by the `f4`-orbit `OP^2` tangent `= Delta_9 = 16` at a rank-one idempotent, leaving open only coherent-state localization from full CHO dynamics; `compute/epsilon_action_selection.py` then removes the rank-one INPUT on the configuration-space side by proving the Freudenthal sharp IS the gradient of the `E6`-invariant cubic norm (`X#=grad N3`, finite-difference match `~1e-8`), so the rank-one variety is the critical/global-minimising locus of `N3` -- on the physical slice `{O>=0, Tr O=1}` AM-GM gives `N3 in [0, 1/27]` with the rank-one idempotents the global MINIMISERS (`N3=0`, flat exactly along the `f4`-orbit `OP^2`) and `I/3` the unique maximiser (`1/27`), and the SAME `N3` whose symmetry group `E6` Schur-forces the flat `1/27` measure now also selects the ray, reducing 'rank-one assumed' to 'rank-one = minimiser of the invariant cubic potential' with the action's potential-origin + kinetic coefficient + EoM still open) from the action/one-operator dynamics | `foundations/02_action.md` writes down an explicit free-particle-plus-Wess-Zumino action on the rank-one transition sphere and shows the `pi` holonomy is the Berry phase of its unique closed geodesic (great circle), upgrading `SPURION_BRIDGE.md` Block 4 from "minimal geometric loop" to "stationary configuration of `S`" (verified in `compute/action_derivation.py`). Three triangulating experiments then sharpen the `432 = 16*27` trace space (R3): `compute/epsilon_heat_kernel.py` rules OUT a heat-kernel origin for the `pi` (it would appear as `(4 pi)^{-d/2}`, never a bare numerator `pi`) and rules IN the geometric/Berry origin, forcing the `432` to be a pure STATE COUNT; `compute/epsilon_cubic_discriminant.py` rules OUT the universal cubic-discriminant `27` (a rank-one breaking gives a double root, `Delta=0`), leaving the `27` as `dim J3(O)`; `compute/epsilon_state_count.py` DERIVES `16 = dim OP^2 = F4/Spin(9)` numerically as the dimension of the rank-one idempotent manifold of `J3(O)` (Jacobian nullity `16` at all three primitive idempotents). `compute/epsilon_product_space.py` then stratifies `27 = 1 + 16 + 10` and shows the geometric `16` is the off-diagonal octonion pair INSIDE the flavour `27`, so the external product `A_Weyl x J3(O) = 432` is exact iff `A_Weyl ~= T(OP^2)` as Spin(9) spinors. `compute/epsilon_weyl_isomorphism.py` then DISCHARGES that isomorphism: it builds `f4 = Der(J3(O))` (dim 52) and the idempotent-stabiliser `spin(9)` (dim 36, semisimple) from the octonion table, proves the `F4/Spin(9)` isotropy action on `T(OP^2)` is irreducible of real type (commutant 1 = the spinor `Delta_9`) and that an independent octonionic `Cl(9)` gives the same real 16 spinor (also commutant 1, same Casimir structure `1,9,126`), then closes the subgroup seam to a frame choice by recovering the flavour Clifford system and proving gauge/flavour `Spin(9)` are `O(16)`-conjugate (`compute/epsilon_spin9_embedding.py`). `compute/epsilon_rank_one_kernel.py` reframes R1: rank one = primitive idempotent = pure single-generation vacuum, dual to `N_gen=3` (residual: vacuum purity). `compute/epsilon_free_action.py` reframes R2: with only two-level symmetry, invariant potential is constant, invariant metric is unique up to scale, and `theta=pi` is scale-invariant, so the free action + topological term is forced up to a `theta`-irrelevant scale; `compute/epsilon_a4_two_level.py` derives that two-level symmetry from the `SU(2)` closure of `A4` (via `Q8` as the lifted `V4`, Burnside irreducibility, and `A4/V4=Z3`), leaving only the origin of the `A4` action as hypothesis (H). `compute/epsilon_channel_coefficients.py` derives the mass-sector channel ranks `(1, 3, 8)` as number-operator Fock-grade traces on the octonionic ladder (`up=Tr P_0=1`, `down=Tr P_1=3`, `lepton=Tr I_Fock=2^3=8`), closing M3; and `compute/epsilon_vcb_halfangle.py` derives the `|V_cb|` coefficient `1/2` as the `SU(2)` spinor half-angle (`sin(eps0/2)`, finite avatar `tan(pi/8) = sqrt(2)-1`). Close the remaining residuals or demote F0 to ansatz |
| M1 | Charm mass | `m_c = epsilon_0^2 m_t` | Open bridge / partially derived projector | `sector_projector_derivation.py` and `compute/epsilon_channel_coefficients.py` give the grade-0 singlet rank `1 = Tr P_0` (number-operator Fock-grade trace); derive why the Yukawa map selects that grade |
| M2 | Strange mass | `m_s = 3 epsilon_0^2 m_b` | Open bridge / partially derived projector | `sector_projector_derivation.py` and `compute/epsilon_channel_coefficients.py` give the grade-1 color-triplet rank `3 = Tr P_1` (number-operator Fock-grade trace); derive the Yukawa trace |
| M3 | Muon mass | `m_mu = 8 epsilon_0^2 m_tau` | Derived bridge / Fock trace | `compute/epsilon_channel_coefficients.py` derives the lepton rank `8 = Tr I_Fock = 2^3 = dim Lambda^*(C^3)` — the full octonionic-ladder Fock module trace, not a hand-chosen rank; M3 closed for the mass sector |
| M4 | Tau mass | `m_tau/m_t = sqrt(2) epsilon_0^2` | Open bridge | Derive the tau alignment and normalization directly from CHO Yukawa structure |
| M5 | Bottom mass | `m_b/m_tau = 7/3` | Open bridge / scale-dependent | `compute/mass_ratio_rg_audit.py` shows this is the ONE headline mass relation that is NOT 1-loop RG-invariant: `m_b` runs under QCD while `m_tau` does not, so `7/3` holds only near `mu ~ m_b` (0.8% there, drifting to `-26%` at `M_Z` and `-70%` at the GUT scale). The relation must therefore be quoted AT a matching scale. Derive `dim(Im O)/N_c` as an operator ratio, not only a counting rule, AND state the scale at which it is meant to hold |
| M6 | Inter-sector ratio | `m_s m_t/(m_b m_c) = 3` | Derived bridge / dependent | Track scheme dependence and covariance with `m_s`, `m_b`, `m_c`, `m_t` |
| M7 | Inter-sector ratio | `m_mu m_t/(m_tau m_c) = 8` | Derived bridge / dependent | Same covariance treatment as M6 |
| M8 | Georgi-Jarlskog ratio | `m_mu m_b/(m_tau m_s) = 8/3` | Derived bridge / dependent | Same covariance treatment as M6 |
| M9 | Up mass | `m_u = (1/4) m_c^2/m_t` | Open bridge / candidate operator, compounding | `CHO_OPERATOR.md` embeds the `1/4` weak projector shape; derive uniqueness. Squared ratio of predicted masses; intrinsic factor error ~8% but loosely constrained (exp error ~20%). See `first_generation_audit.py`. `compute/sector_sphere_dichotomy.py` reads `1/4 = (Tr P_0/2)^2` from the derived grade-0 Fock rank and isolates the M9/M10/M11 shared discriminant (`pi` <=> CONTINUOUS colour-singlet sphere; rational <=> DISCRETE coloured Fock grade); the colour-singlet->continuous SELECTION stays open |
| M10 | Down mass | `m_d = (9/4) m_s^2/m_b` | Open bridge / candidate operator, compounding | Candidate gives `(1/4) N_c^2`; derive the sector-square rule. Intrinsic factor error ~0.6%, propagation ~ -4.7%. See `first_generation_audit.py`. `compute/sector_sphere_dichotomy.py` derives exactly this sector-square rule as `9/4 = (Tr P_1/2)^2 = (1/4) N_c^2` with `N_c = Tr P_1 = 3` (the derived grade-1 Fock rank); the SELECTION onto a single discrete grade (vs the lepton continuum) is still open |
| M11 | Electron mass | `m_e = (1/4 pi) m_mu^2/m_tau` | Open bridge / shape factor identified, dominant outlier | `compute/epsilon_mixing_coefficients.py` IDENTIFIES the lepton `1/(4 pi)` as the uniform-measure normalization on the transition Bloch sphere `S^2` (`Int dOmega = 4 pi`) — the unique `pi`-carrying shape, partner of the full-Fock lepton trace `8` (the up/down shapes `1/4`, `9/4` are weak-isospin squares with no `pi`). This sharpens M11 from "chosen angle density" to "the transition-sphere measure", but the dynamical REDUCTION of the lepton Yukawa trace to that uniform average is still open. `compute/lepton_yukawa_action.py` (Item 2) then FORCES `1/(4 pi)`: it is the SU(2) invariant-average (Schur) normalization `= 1/(total solid angle)` on the SAME `S^2` whose hemisphere solid angle `2 pi` gives the Berry `theta = pi` of `foundations/02_action.md`, and it assembles the charged-lepton Yukawa as ONE Hermitian operator `m_tau * diag(1, 8 eps0^2, (1/4pi)(8 eps0^2)^2)` reusing the derived Fock trace `8` (M3) and the rank-one cascade square — upgrading `1/(4 pi)` from "identified" to "forced". Still the `-3.75 sigma` audit outlier: it can NEVER be a precision claim until the sphere-vs-discrete sector resolution and the trilinear-from-EoM are derived; otherwise demote it. See `first_generation_audit.py`. `compute/sector_sphere_dichotomy.py` then isolates WHY the lepton alone carries `pi`: a finite-group (`Q8`) average of the rank-one projector is EXACTLY `I/2` (rational, no `pi`) while the continuous sphere average is `1/(4 pi)`, so the colour-singlet lepton (full continuous Fock module) is forced into the `pi`-bearing regime and the coloured quarks (single discrete Fock grade) into the rational one; the colour-singlet->continuous SELECTION remains the open input |
| C1 | Cabibbo angle | `|V_us| = sqrt(7) epsilon_0` | Derived bridge / Fano count | `compute/epsilon_mixing_coefficients.py` derives `7 = dim Im(O) =` all Fano lines; `|V_us|` is an amplitude (power 1 in eps), so it scales as `sqrt(7)` by the amplitude-vs-probability rule (RMS of a 7-direction random-phase sum, Monte-Carlo verified). Build and diagonalize the CHO mass matrices to confirm |
| C2 | CKM 2-3 mixing | `|V_cb| = epsilon_0/2` | Derived bridge / SU(2) half-angle | `compute/epsilon_vcb_halfangle.py` derives the `1/2` as the spin-1/2 HALF-ANGLE of the `SU(2)` double cover of the transition Bloch sphere: a single-qubit (inter-generation) transition amplitude is `sin(eps0/2) ~ (1/2) eps0` (coefficient `1/2`), while the `Im(O)` VECTOR channel of `|V_us|` carries the full angle `sin(eps0)` (coefficient `1`, summed to `sqrt(7)`). The contrast `sqrt(7)` vs `1/2` is vector-vs-spinor, its finite avatar `tan(pi/8) = sqrt(2)-1`. The VALUE `1/2` is no longer a weak-isospin input; the remaining residual is the channel ASSIGNMENT (which channel is the spinor) |
| C3 | CKM 1-3 mixing | `|V_ub| = (sqrt(2)-1)|V_us||V_cb|` | Open bridge / half-angle identity verified | `compute/epsilon_vcb_halfangle.py` verifies `tan(pi/8) = sqrt(2)-1` exactly and shows it is the transition/survival ratio `tan(theta/2)` at the octonionic 45 deg reflection (the same spinorial half-angle that linearises to the `1/2` of C2). Still to derive: that `|V_ub|` carries this subleading half-angle phase factor from the CHO mass matrices |
| C4 | Jarlskog invariant | `J = 3.01e-5` | Open bridge / candidate operator | `CHO_OPERATOR.md` derives the Fano phase and shows the Fritzsch placement gives the right J; derive corrected CKM magnitudes and J from one matrix |
| N1 | Heaviest neutrino mass | `m_nu3 = v^2/(2 M_P/3^9)` | Demoted bridge / order-1 consistent (Item 4) | `compute/predict_neutrino_sum.py` (`floor_violation()`) shows the CHO value `m_nu3 = 48.9 meV` sits `1.2 meV` BELOW the normal-ordering floor `sqrt(Delta m31^2) = 50.1 meV`, which at the floor's experimental error alone reads as `~4.6 sigma`. `compute/neutrino_floor_resolution.py` (Item 4) shows that `4.6 sigma` assigns ZERO theory error to a TREE-LEVEL seesaw: its two inputs are derived only up to an `O(1)` normalization, calibrated from their SISTER rows — `M_R = M_P/3^9` (sister `M_W = M_P/3^36`, `+1.2%`) and `y_nu3 = 1` (sister `y_t = 1`, `+0.8%`, entering squared). The folded theory error is `~2.0%` (`+/- 1.0 meV`), so the honest significance is `1.2 sigma` — consistent, NOT a clean kill. DECISION: N1 is DEMOTED from a precision prediction to order-1 consistency; the central value still sits just BELOW the physical floor (stated plainly, not hidden), and the integer exponent `9` is fine (it would shift by only `+0.023`). LIVE OBLIGATION: derive the `O(1)` seesaw normalization AND the `y_nu3 = 1` saturation; GENUINE KILL: a pinned, DERIVED normalization that still falls below the floor beyond the (then-shrunk) theory error |
| N2 | Neutrino splitting ratio | `Delta m21^2/Delta m31^2 = 4 epsilon_0^2` | Derived bridge / Fano count | `compute/epsilon_mixing_coefficients.py` derives `4 =` Fano lines AVOIDING the vacuum point `e7` (`7 - 3`); a splitting probability (power 2 in eps) scales as the plain count `4`. Derive `DeltaY` dynamically to confirm the residual symmetry |
| N3 | PMNS reactor angle | `sin^2(theta13) = 3 epsilon_0^2` | Derived bridge / Fano count | `compute/epsilon_mixing_coefficients.py` derives `3 =` Fano lines THROUGH the vacuum point `e7` (the SU(3) colour/stabiliser triplet); a probability (power 2) scales as the plain count `3`. Same `DeltaY` target as N2 |
| N4 | PMNS solar angle | `sin^2(theta12) = 1/(3 + sqrt(7) epsilon_0)` | Open bridge / candidate operator | Derive quark-lepton complementarity from the shared triality operator |
| N5 | PMNS atmospheric angle | `sin^2(theta23) = 4/7` | Derived bridge / Fano count, future sensitive | `compute/epsilon_mixing_coefficients.py` derives `4/7 =` (lines avoiding the vacuum) / (all Fano lines) `= 4/7` directly from the octonion Fano incidence table. **Item 7 (the single sharpest falsifiable claim):** `compute/theta23_octant_prediction.py` is the dedicated forward-test analysis. It stakes `sin^2(theta23) = 4/7 = 0.5714` (`theta23 = 49.1 deg`, UPPER octant) as the sharpest CHO bet for three reasons: (i) it is the ONLY mixing prediction that is an exact rational and is `eps0`-INDEPENDENT (verified: `d sin^2 theta_23 / d eps0 = 0` exactly, while `sin^2 theta_13 = 3 eps0^2` moves), so it does not touch the open `pi/432` seam; (ii) the octant is fixed by the SAME integers as the value — the lower-octant mirror is exactly `cos^2 = 3/7`, and `4` lines avoiding the vacuum `> 3` through it IS "upper octant", so the octant measurement is a direct test of which Fano class controls `theta23`; (iii) the octant is currently UNRESOLVED (T2K/NOvA tension), making this a genuine pre-registered bet decided this decade by DUNE / Hyper-Kamiokande. KILL: a stable lower-octant resolution (`sin^2 theta23 < 1/2`) falsifies the Fano assignment; the Fano-count -> angle map remains a derived bridge, not a closed theorem. Forward test only — no row promoted, Bayes factor untouched; the frozen registry (Q2) stays authoritative |
| CP1 | Strong CP | `theta_bar = 0` | Derived bridge | Formalize Fano parity as a symmetry of the QCD path-integral measure |
| Q1 | One-generation electric charges | `Q in {0, 1/3, 2/3, 1}` with colour multiplicities `(1,3,3,1)` | Derived (Furey / Dubois-Violette), reproduced numerically | `ladder_charges.py` (Lever C) builds three nilpotent ladder operators by left-multiplication in `C x O`, verifies the Witt-basis relations (`alpha^2=0`, `{alpha_i,alpha_j^dag}=delta_ij`), and shows the number operator `Q=(1/3) sum alpha_k^dag alpha_k` has spectrum `{0, 1/3, 1/3, 1/3, 2/3, 2/3, 2/3, 1}` -- charge quantisation in thirds and the SM colour multiplets are OUTPUTS, not inputs. The `C x H x O` extension (weak isospin + hypercharge) is now done -- see Q2 |
| Q2 | One-generation weak isospin + hypercharge | weak `SU(2)` doublet `T_3 = +-1/2`; full SM hypercharge spectrum `Y in {1/3, 4/3, -2/3, -1, -2, 0}` | Derived (weak isospin + GMN), reproduced numerically; chiral projection open | `weak_isospin_hypercharge.py` (Lever D) builds weak `SU(2)` as the LEFT-multiplication algebra of the quaternions `H < O` (Fano line e1,e2,e3): `T_a=(i/2)L_(e_a)` satisfy `[T_a,T_b]=i eps T_c`, Casimir `3/4` (j=1/2), `T_3` eigenvalues `+-1/2` -- a weak doublet, no isospin by hand. Left/right quaternion mults commute and span `so(4)=su(2)_L (+) su(2)_R` (weak `SU(2)` is one chiral factor). On `C x H x O` the weak generators `T_a (x) I` commute with the Lever-C colour/charge operator and the Lever-B chirality (direct product `SU(2)_w x SU(3)_c`; a weak rotation does not flip octonionic chirality). The Gell-Mann-Nishijima formula `Y=2(Q-T_3)` with `Q` from Lever C (O) and `T_3` from H then reproduces the entire bizarre SM hypercharge spectrum of one 16-fermion generation EXACTLY. Open (one named seam): why only LEFT-handed fields are doublets and right-handed fields are isospin singlets -- the chiral projection that singlets the RH sector is Lever B's KO-dim-6 real structure; closing the Lever B <-> Lever D linkage is the remaining obligation. Written up as a proof document `foundations/05_electroweak_su2_theorem.md`: Theorems 1-3 (su(2) doublet from H, the so(4)=su(2)_L(+)su(2)_R chiral split, the direct product + simultaneous diagonalisation) are CLOSED theorems with hand proofs + machine witnesses; Proposition 4 (the SM hypercharge spectrum) is honestly stated as CONDITIONAL on the chiral assignment (C) -- the residual is reduced from six unexplained charges to one chiral projector. The DICHOTOMY half of (C) is now closed in `foundations/06_chiral_idempotent.md` + `compute/chiral_projector.py`: the gauged generators G_a = T_a (x) P_L with the single KO-6 idempotent P_L = (I+gamma_Q)/2 close su(2) (idempotency carries the bracket), give doublet on gamma_Q=+1 and singlet on gamma_Q=-1, and [Q, gamma_Q]=0 (alignment refinement of Lever B: gamma_Q is the volume element over the six charge-carrying directions, dropping the colour axis). Residual narrows to orientation + the field-by-field content map (G1) |
| Q3 | One-generation physics-map anomaly audit | 16 Weyl states including sterile `nu_R`; `SU(3)^2-U(1)`, `SU(2)^2-U(1)`, `U(1)^3`, `grav^2-U(1)`, and Witten `SU(2)` anomaly checks vanish | Derived bookkeeping / anomaly-clean; full content map open | `compute/physics_map_audit.py` freezes the public one-generation table, cross-checks `Q` against Lever C, `T3` against Lever D, `Y=2(Q-T3)` against the GMN table, verifies `[Q,gamma_Q]=0`, and computes exact rational anomaly coefficients in the all-left-handed Weyl basis. This closes the consistency/anomaly bookkeeping gap but not the functorial map from the three `J3(O)` frame idempotents / `T(OP2)` tangent spinors to field labels, and not the Yukawa operator. |
| K1 | Internal KO-dimension | `KO-dim = 6 (mod 8)` | Derived (necessary chirality condition), reproduced numerically | `ko_dimension_chirality.py` (Lever B) builds the left-multiplication Clifford algebra `Cl(0,7)` from the octonion table, the chirality grading `gamma=i L_1..L_6` (`gamma^2=+1`, 4+4 Weyl split) and the real structure `J=` complex conjugation, and computes `(epsilon, epsilon'')=(+1,-1)` -> KO-dimension 6, the SAME value Connes' NCG Standard Model needs for chirality WITHOUT fermion doubling -- the structural cure for the mirror pair that downgrades G1. Open: this fixes only the internal Clifford module; the Dirac operator D (epsilon') and a chiral anomaly-free one-generation spectrum are not yet specified |
| CC1 | Cosmological constant | `Lambda^(1/4) = (11/12) M_P/(sqrt(2) 3^64)` | Open bridge | Derive free-energy factorization and the `11/12` screening factor without tuning |
| GR1 | Emergent metric from non-associativity | `g_(mu nu)(a,b) = <[e_mu,a,b],[e_nu,a,b]>`, `tr g = 16 (|a|^2|b|^2-<a,b>^2)` | Exploratory side project / kinematic M-GRAV brick | `gravity_curvature.py` builds the metric perturbation as the Gram pullback of the associator transport defect `M_(a,b)(x)=[x,a,b]` and PROVES numerically it is symmetric, positive-semidefinite, and `G2`-covariant (`g(Ra,Rb)=R g R^T` exact on all 1344 finite signed-permutation automorphisms) -- a genuine rank-2 tensor under the structure group with NO hand-inserted geometric input, replacing the placeholder `graviton.py`. The curvature is purely imaginary (transverse); the scalar density obeys an area law with the SAME `16 = dim OP^2` as F0; by alternativity `g` annihilates its source labels (rank 4, transverse graviton mode), and its 3-dim null space is exactly the associative quaternionic subalgebra `span{a,b,Im(ab)}` (Artin made geometric). Spacetime side: `C x H = M_2(C)` Hermitian gives Minkowski `R^(3,1)` with `SL(2,C)` Lorentz. `compute/gravity_gate_audit.py` is the Phase 5 decision gate: the internal metric has signature `(4,0,3)`, the finite `G2` witness has commutant dimension `1`, and any rank-four projector averages to `(4/7)I`, so no canonical 4D spacetime plane is selected. Even a hand-picked four-plane remains positive-semidefinite, and no field equation/Newton constant emerges. Gravity therefore stays out of scope for the present framework unless a future construction supplies the missing Lorentzian bridge and dynamics. |
| D1 | No WIMP/axion/SUSY/proton decay | Null exclusion claims | Future test | Tie each claim to mass/coupling/lifetime reach and state lower evidential weight than positive quantitative targets |
| STAT1 | 25-row audit table | Median error and pull summary | Descriptive diagnostic / covariance closed / Bayes factor computed | `covariance_gof.py` builds the full covariance with a shared-`eps0` common mode: the 22 independent rows collapse to `N_eff ~ 10` effective observables, and the correlated reduced `chi^2 ~ 1.8` against `N_eff` (`p ~ 0.06`) -- consistent but borderline, superseding the diagonal-with-floor figure and the raw row count. `bayesian_evidence.py` goes further: it charges CHO the full Occam price for every prefactor it CHOOSES and credits nothing for those it DERIVES. `scoreboard.py` reads the resulting Bayes factor as one number across a credit ladder: with only `8/3` closed (the pre-`eps0`-program state) `ln B ~ -21` (FAVOURS the O(1)-numerology null, the skeptic's strongest point); crediting today's numerically-closed prefactors (the Fock-grade ranks `1,3,8`, the Fano-line counts `sqrt(7),3,4/7`, the `SU(2)` half-angle `1/2`) lifts it to `ln B ~ -3`; crediting the geometric `pi/432` as well lifts it to `ln B ~ +6` (FAVOURS CHO). The sign-flip is pinned to a single named seam -- whether `pi/432` is geometrically forced -- so the verdict is now a sharp, falsifiable question rather than a free knob. `per_row_theory_error.py` (Item 6) replaces the single global `1.5%` floor with a theory error keyed to each row's weakest-bridge status, tightening a row ONLY when its bridge is derived: the one fully `eps0`-independent exact theorem `sin^2 theta_23 = 4/7` earns a precision test and passes (exp pull `-0.02`), while the `eps0`-ladder rows `m_s`, `m_mu` are shown to be `~1.5-2%` relations (a sub-percent `0.5%` claim is falsified at `>2 sigma`) and the continuum rows `alpha`, `sin^2 theta_W` stay derivation-limited and are excluded rather than inflated -- a goodness-of-fit refinement only, no row promoted and the Bayes factor untouched |

## Highest-Value Next Proofs

1. Prove that the candidate operator in `CHO_OPERATOR.md` is forced by the CHO action or representation theory.
2. Derive the rank-one epsilon transition, sector projectors, and shape factors inside that one operator.
3. Reconcile CKM corrected magnitudes with the Fritzsch-level Jarlskog phase placement in one charged-Yukawa diagonalization.
4. Resolve the PMNS gap in `OPERATOR_GAP_AUDIT.md`: derive `DeltaY` dynamically and clarify the residual symmetry beyond simple cyclic `Z3`.
5. Rework the statistics around an independent observable set with covariance for mass-derived ratios.
6. Close the continuum/RG matching gate in `compute/rg_matching_audit.py`: derive the matching scale, VP/threshold scheme, and electroweak normalization rather than inverse-matching them.
7. Reopen gravity only if `compute/gravity_gate_audit.py` can be beaten: derive a canonical 4D Lorentzian reduction and dynamics without a hand-picked four-plane.

## Bridge Artifacts

- `compute/flavour_derivation.py` — first scaffold for M9-M11, C1-C4, and N2-N5. It derives charged-sector NNI bridge factors, builds CKM/PMNS unitary matrices, and exposes the remaining CKM Jarlskog phase-placement task.
- `compute/epsilon_bridge.py` — diagnostic scaffold for the `epsilon0^2 = pi/432` trace target, empirical bridge estimates, nearby trace normalizations, and proof obligations.
- `ACTION_PROJECTOR_BRIDGE.md`, `PRIMITIVE_PROJECTOR_BRIDGE.md`, `compute/action_projector_derivation.py`, and `compute/primitive_projector_derivation.py` — action/projector diagnostics showing rank-one Fano support, the Fano-pair automorphism orbit, and the normalized action rank penalty that conditionally selects the primitive `A_Weyl x J3(O)` product.
- `EPSILON_BRIDGE.md` — companion memo defining the `epsilon0^2` bridge state space, proposed trace formula, failure modes, and next proof steps.
- `foundations/08_epsilon_measure_theorem.md` and `compute/epsilon_measure_audit.py` — Phase 2 theorem gate for F0: states the conditional normalized-transition-trace theorem with hypotheses H1-H5, computes `pi/(16*27)` under those hypotheses, and checks nearby alternatives (rank `r>1`, `OP2` alone, `J3(O)` alone, real algebra, traceless Jordan, raw reciprocal, full turn, Fano-pair degeneracy, and unnormalized sphere area). This narrows the live residual to H4, the invariant normalized measure; it does not promote F0 from `GEOMETRIC` to `DERIVED`.
- `compute/epsilon_measure_schur.py` — Phase 2 Schur witness for F0: shows the `1/16` and `1/27` weights in `epsilon0^2 = pi/432` are FORCED by irreducibility, not chosen. By Schur's lemma the invariant average of a rank-one projector on an irreducible `d`-dim module is the flat `(1/d) I`. It witnesses, with the project's own group-theory machinery: `SO(5)` on `R^5` (sanity, `-> I/5`); `Spin(9)` on `Delta_9` irreducible (commutant `1`) `-> 1/16`; `F4 = Der(J3O)` on the `27` REDUCIBLE (commutant `2`, `27 = 1 + 26`, identity over-weighted at `1/3`), so `F4` alone is insufficient; and the full `E6 = f4 (+)` traceless-`L_X` (dim `78`, closes under bracket, residual `2e-14`) IRREDUCIBLE (commutant `1`) `-> 1/27` to machine precision. `E6` is the reduced structure group preserving the cubic norm `N3` (the Freudenthal determinant) — the SAME invariant as the `generation_cascade` seesaw. This closes the NORMALIZATION half of H4 (the flat `1/dim` weights are now a Schur theorem); the live residual narrows to why the phase space is the PRODUCT `Delta_9 (x) J3(O)` (a representation identification). It does NOT promote F0 from `GEOMETRIC` to `DERIVED`.
- `compute/epsilon_phase_space_product.py` — Phase 2 product witness for F0: sharpens the residual from a vague "why product?" to one explicit seam, Assumption P (independent commuting gauge/flavour sector actions with minimal multiplicity). Under P, the canonical carrier is `Delta_9 (x) J3(O)`, and the factorized invariant average of a product rank-one projector is exact: `(I_16/16) (x) (I_27/27) = I_432/432`, giving `pi/432` mechanically. This closes normalization + factorization UNDER sector independence, while keeping F0 open because deriving P from the CHO action/one-operator dynamics remains outstanding.
- `compute/epsilon_assumption_p_gate.py` — Phase 2 Assumption-P action/operator gate for F0: directly tests the current one-operator scaffold and finds exact `16x27` separable primitive structure for the epsilon bridge operator (operator-Schmidt rank `1`, machine-precision product reconstruction, normalized trace `pi/432`). A stress test adding an independent second product term raises Schmidt rank to `2`, confirming the gate detects real cross-sector mixing. This is strong operator-level evidence for Assumption P, but still not an action derivation; F0 remains open until the separable primitive kernel is produced dynamically from CHO action/one-operator equations.
- `compute/epsilon_product_irreducible.py` — Phase 2 product-irreducibility witness for F0 that REDUCES the clauses of Assumption P: it proves `Delta_9 (x) J3(O)` is irreducible under the factor-wise product group `Spin(9) x E6` (computed cheaply by partial trace, `<P>_{G1 x G2} = (1/16) I_16 (x) <Tr_1 P>_{E6}`, never a `432x432` element), so by Schur ANY rank-one spurion averages to `I_432/432`. A maximally entangled spurion (operator-Schmidt rank `16`) and a separable one (rank `1`) give the SAME flat average to machine precision (`5e-18`), which breaks the circularity of `epsilon_assumption_p_gate.py` (whose scaffold operator was BUILT separable): flatness does NOT depend on separability or minimal multiplicity. An `F4` control (`F4` leaves the `27` reducible, residual `1e-2`) shows the factor-wise `E6` is necessary. Net: Assumption P collapses from three clauses to one — "factor-wise `Spin(9) x E6` invariance" — but F0 is NOT promoted, because deriving that product arena from the CHO action remains the live seam.
- `compute/epsilon_symplectic_volume.py` — Phase 2 symplectic/orbit-method route for F0 that attacks the surviving "why a product" clause with a NEW lens: geometric quantization. It recomputes `16` and `27` from scratch with the Weyl dimension formula as the Bohr-Sommerfeld / Borel-Weil state counts of single coadjoint orbits (the `Spin(9)` spinor orbit and the `E6` minimal orbit; method checks: `so(9)` vector `9`, adjoint `36`; `E6` adjoint `78`), and verifies on small product root systems (`A1xA1 -> 4`, `A2xA1 -> 6`, `A2xA2 -> 9`) that the quantization is MULTIPLICATIVE over a direct-product group. So the Liouville volume of the product orbit is `16 x 27 = 432` as a THEOREM of the orbit method: "the arena is a product" follows from "the symmetry is a product group", it is not an extra assumption. The bare `pi` (not `2 pi`) is recovered as the symplectic half-flux of the minimal transition orbit `CP^1 = S^2` (reusing the great-circle Berry phase of `compute/action_derivation.py`), giving `pi x (1/432) = pi/432`. Honest caveat stated in-module: `OP^2 = F4/Spin(9)` is itself NON-symplectic, so the carriers are the orbits in `so(9)^*` and `e6^*`, not `OP^2`. F0 is NOT promoted: the live seam becomes the sharper "which two coadjoint orbits does the CHO action quantize", with a positive geometric mechanism for the product replacing the assumed commuting split.
- `compute/epsilon_factor_forcedness.py` — Phase 2 factorization-forcedness audit for F0 (the `look_elsewhere` discipline applied to the split itself). It enumerates all ten factorizations `432 = a x b` and scores each factor by whether the framework ALREADY derives a transition carrier of that dimension; `16 x 27` is the UNIQUE pair whose BOTH factors are independently-derived carriers (`16 = Delta_9 = dim OP^2`, `27 = dim J3(O)`), strictly out-scoring every rival (`8 = dim O`, `4 = dim H`, `36 = dim so(9)`, ... each paired with a structureless partner). A single-group no-go scan then recomputes the fundamental-rep dimensions of `su(n)`, `so(n)`, and the exceptionals `G2, F4, E6, E7, E8` from their root systems and finds `432` among NONE of them, while it IS the `Spin(9) x E6` (spinor)x(minimal) bifundamental — so a PRODUCT realization is forced, not a single simple group. This removes the factorization freedom but does NOT promote F0 (honest scope: it ranks pre-derived structure and is a minimal/fundamental-rep statement, not an absolute no-go, since large non-fundamental irreps of dimension `432` do exist).
- `compute/epsilon_action_stationary.py` — Phase 2 variational action witness for F0: in the normalized link-action class (`O>=0`, `Tr(O)=pi`), the primitive kernel `O=pi|tau><tau|` is the unique global maximizer of `S_link = log(<O,K>/(||O||_F ||K||_F))` by Cauchy equality; the current scaffold epsilon operator saturates this bound exactly, while random PSD/separable stress scans remain strictly below. This sharpens Assumption P support from "operator-separable" to "variationally selected in class". It still does NOT close F0: deriving the physical transition ray and admissible kernel class from full CHO dynamics remains open.
- `compute/epsilon_action_selection.py` — Phase 2 action-selection witness for F0 that removes the rank-one INPUT of `epsilon_action_stationary` on the configuration-space side. It proves the Freudenthal sharp IS the gradient of the `E6`-invariant cubic norm, `X# = grad N3` (finite-difference `dN3|_X(Y)` matches the trace-form `<X#,Y>` to `~1e-8`, and `N3 = det =` product of the three `J3(O)` eigenvalues), so the rank-one variety `X#=0` is precisely the critical locus of `N3`. Trace-constrained criticality `X#=lam I` has ONLY the solutions `{rank-one}` (`lam=0`) and `{central cI}` (`lam=c^2`); on the physical slice `{O>=0, Tr O=1}` AM-GM bounds `N3 in [0, 1/27]` with the rank-one idempotents the global MINIMISERS (`N3=0`, the minimum flat exactly along the `f4`-orbit `OP^2`, tangent dim `16`, and rising into the full-rank bulk `diag(1-2t,t,t)`) and `I/3` the unique maximiser (`N3=1/27`). An `F4`-automorphism scan confirms `N3` invariance (`~1e-15`) and an `E6`-boost scan confirms the rank-one variety is `E6`-stable (`~1e-13`). Interlock: the SAME `N3` whose reduced structure group `E6` Schur-forces the flat `1/27` measure (`epsilon_measure_schur`) also selects the ray via its minimisers (the value `1/27 = (1/3)^3 = 1/dim` coincides because `dim J3(O) = 27 = 3^3`, quoted but not leaned on). The great-circle Berry phase `pi` (`action_derivation`) supplies the candidate action's kinetic angle. Net: 'rank-one assumed' is upgraded to 'rank-one = global minimiser of the invariant cubic potential', but F0 is NOT promoted — deriving that the CHO action's potential IS this `N3`, the kinetic coefficient, and the full equations of motion remain open, and no Bayes credit moves. It takes the two-level Bloch sphere `S^2 = CP^1` of `foundations/02_action.md` and shows the SAME sphere supplies both numbers the lepton sector needs: its hemisphere solid angle (`2 pi`) is the Berry `theta = pi` (Bargmann great-circle holonomy), and its total solid angle (`4 pi`) is the SU(2) invariant-average (Schur) normalization, so the first-generation shape factor `k_l = 1/(4 pi)` is FORCED (not chosen) as `1/(total solid angle)`. Reusing the derived Fock trace `8` (M3) and the rank-one "lift exactly one level" cascade, it builds the Hermitian operator `Y = m_tau * diag(1, 8 eps0^2, k_l (8 eps0^2)^2)`, whose spectrum lands `tau` exactly, `mu/tau` at `-2.2%`, and `e/tau` at `-6.3%` (the known M11 first-generation outlier, reported honestly — only `mu` is asserted). Net: `1/(4 pi)` is upgraded from "identified" to "forced" and the lepton Yukawa is now ONE operator end-to-end, but F0 is NOT promoted and the seams stay open — WHY leptons take the continuous-sphere average while quarks take discrete weak-isospin projections (`1/4`, `9/4`, no `pi`), the trilinear from the CHO equations of motion, and the `~6%` `m_e` residual.
- `compute/f0_vacuum_majorization.py` — Phase 2 vacuum-robustness witness for F0 that strengthens `epsilon_action_selection` from one chosen functional to a universality class. On the physical slice `{O>=0, Tr O=1}` the rank-one transition ray is the MAJORIZATION-MAXIMAL state: its spectrum `(1,0,0)` majorises every state and the maximally-mixed centre `I/3` is majorised by every state (verified on 4000 samples), and `F4` automorphisms preserve the `J3(O)` spectrum (`~1e-12`) so every spectral functional reduces to the eigenvalue simplex. By Hardy-Littlewood-Polya this single order fixes the extremiser of the WHOLE Schur class at once: a panel of strictly convex `g` (purity `l^2`, `l^4`, `exp l`) is maximised at the rank-one ray and minimised at `I/3`, and a panel of strictly concave `g` (von Neumann entropy, `sqrt`, `-l^2`) is reversed — so the cubic norm `N3=det` (Schur-concave, min `0` at rank-one, max `1/27` at `I/3`), the entropies (min `0` / max `log 3`), AND the Connes finite-spectral-action term `-a Tr Phi^2 + b Tr Phi^4` (Schur-convex, symmetry-breaking minimum at the rank-one ray) ALL select the same vacuum. So the F0 vacuum is robust to the choice of action, and a Connes-type spectral action (the same KO-dim-6 triple as `ko_dimension_chirality`/`spectral_action`) lands on the rank-one ray. Honest corrective: the spectral potential is EVEN (degrees 2 and 4) while `N3` scales as `t^3` (degree 3) — different functionals agreeing only on the vacuum. **Open (not faked):** majorisation fixes the vacuum DIRECTION; it does not derive which action CHO realises, the kinetic coefficient on the Berry `pi`, or the `pi/432` normalisation; F0 is NOT promoted and no Bayes credit moves.
- `compute/f0_spectral_triple_gate.py` — Phase 1.1 of the gold-standard roadmap (`ROBUSTNESS_ACTIONS.md`): the first attempt to ASSEMBLE the finite Connes spectral triple `(A, H, D, J, gamma)` whose spectral action would have to output `eps0^2 = pi/432` as its `a4/a2` heat-kernel ratio (the decisive F0 test). It builds three pieces and stress-tests the Connes-Chamseddine axioms numerically. [A] The `C^8` SPIN BRICK from octonion left-multiplications: the grading `gamma8` satisfies `gamma8^2 = I` and is Hermitian, and `J8 = ` complex conjugation gives `(eps, eps'') = (+1, -1)` -> KO-dimension 6 (the Connes chirality value, reused from `ko_dimension_chirality`). [B] The finite YUKAWA as Jordan left-multiplication `L_X` on `J3(O)` (`X = diag(1, 0.6, 0.3)`): self-adjoint, but chirality-EVEN (no native grading, defect `~1.25`), so it must be DOUBLED to `D_F = [[0, L_X],[L_X, 0]]` on `C^54`, which is `gamma_F`-odd with `J_F^2 = +I` but carries KO-dimension 4. [C] The PRODUCT `H = C^8 (x) C^54` (`dim_C = 432`), with `D = gamma8 (x) D_F` self-adjoint, `gamma^2 = J^2 = I`, and `gamma D = -D gamma`. RESULT (honest, with two NAMED obstructions): (1) the ORDER-ZERO axiom `[[D,a],b]=0` literally evaluates the OCTONION ASSOCIATOR — it FAILS for `A = C (x) H (x) O` on the full module (residual `~16`; restricting only the algebra to `H` still fails, `~12`) and is recovered to machine precision (`~1e-15`) ONLY on a genuine associative BIMODULE (algebra AND module both quaternionic) or on the complex line (flexibility `a(xa)=(ax)a`); the order-one axiom fails likewise (`~26`); (2) the chirality-even Jordan-Yukawa doubling makes the product KO-dimension `6 (x) 6 -> 4 (mod 8)`, not 6. So the naive `(A, H, D)` is NOT yet a consistent triple. Both obstructions are STRUCTURAL and known-repairable (rebuild `A` as its associative / special-Jordan envelope; embed the Yukawa in the real structure `J`, not as an even operator) — NOT the F0 KILL — and they LOCALISE the Phase-1.2 prerequisite to "build the spectral action on the associative/special-Jordan envelope with the Yukawa carried by the real structure." The KO-dim-6 spin brick and the metric/real-structure half (`gamma^2 = J^2 = I`, `gamma D = -D gamma`) ARE consistent. **Open (not faked):** F0 stays GEOMETRIC/open; the `pi/432` spectral-action reading is NOT yet established; NO Bayes credit moves and the scoreboard ladder (`-21.3 / -3.2 / +5.6 / +36.2`) is untouched.
- `compute/f0_real_structure_gate.py` — Phase-1.2 PREREQUISITE that sharpens the headline `f0_spectral_triple_gate` order-zero failure into a precise real-structure DICHOTOMY. A finite spectral triple has ONE real structure `J`, and EVERY axiom (KO signs, order-zero, order-one) must hold for that SAME `J`; the Phase-1.1 gate had silently checked the KO-dim-6 signs with `J = ` complex conjugation but order-zero against actual right-multiplication — two different `J`s. Testing both axioms against each candidate `J` on the octonion brick `C^8`: (A) with `J = ` complex conjugation (the KO-6 choice, `B = I`) the opposite algebra `J L_a J^-1 = L_a` coincides with `A`, so order-zero collapses to `[A, A] = 0` and FORCES `A` COMMUTATIVE — the quaternion left-algebra `L(H)` fails (`~14`) and only the abelian complex line `L(C)` holds (`~1e-16`), i.e. no `SU(2)`/`SU(3)`; (B) with `J = ` octonion conjugation (`kappa . conj`, `B = diag(1,-1,...)`) one gets `J L_a J^-1 = -R_a` (genuine right multiplication, identity exact to `0`), so order-zero becomes the associator — it holds on the quaternion BIMODULE (`~1e-15`) and ALLOWS a noncommutative `A`, BUT `J gamma J^-1 = -0.5 gamma` is NOT `+/- gamma` (residual `2.0`), so the grading is no longer `J`-compatible and the KO-dimension is UNDEFINED. So on a single irreducible brick NO real structure delivers BOTH KO-6 AND a noncommutative order-zero algebra — the sharp form of the Phase-1.1 fail. RESOLUTION (computed, the standard Connes route): a nonabelian `A = H` acting on `A (x) A^o` satisfies order-zero EXACTLY (`0`) by left-right commutation while staying genuinely nonabelian (`||[i, j]|| = 2`), so the octonions must GRADE the module (`gamma8`, charges) rather than BE the order-zero `*`-algebra — pointing to the rebuild `A = C (+) H (+) M_3(C)`. Converts the order-zero failure into a precise statement plus a named fix; two open bridges remain (carry out the associative rebuild as a product triple that RESTORES KO-dim 6; embed the chirality-even Jordan Yukawa in the real-structure sector so the finite KO-dim stays 6 not 4). Moves NO Bayes credit: F0 stays GEOMETRIC/open and `eps0^2 = pi/432` is not promoted.
- `compute/f0_associative_triple_gate.py` — Phase-1.2 PROPER: carries out the associative rebuild that `f0_real_structure_gate` named, for the one-generation LEPTON sector. Builds the finite geometry `H = C^8` (basis `[nuR, eR, nuL, eL | ` antiparticles`]`), `A = C (+) H` acting on the left, `J = (`particle`<->`antiparticle swap`) . conj`, `gamma = ` chirality flipped on antiparticles. From explicit matrices: **(A)** order-zero `[a_L, b_R] = 0` holds EXACTLY on `A (x) A^o` for the genuinely NONCOMMUTATIVE summands `H` (`~9e-16`, `||[x,y]|| = 14.7`) and `M_3(C)` (`~2e-15`, `||[x,y]|| = 15.3`) and for the actual SM lepton rep (`~9e-16`) — the colour `M_3(C)` factor commutes with the lepton sector by construction; **(B)** KO-dimension 6 is RESTORED — `J^2 = +I` (`eps = +1`) and `J gamma J^-1 = -gamma` (`eps'' = -1`), chirality WITHOUT doubling, recovering the grading the prerequisite's `kappa.conj` had destroyed; **(C)** an explicit physical Dirac with Dirac Yukawas (`nuR<->nuL`, `eR<->eL`) AND a Majorana mass (`nuR<->nuRbar`) is Hermitian, `gamma`-ODD, `J`-REAL (`J D J^-1 = D`) and satisfies order-one `[[D, a], b^o] = 0` (`~9e-16`), so the seesaw lives in the real-structure sector and the finite KO-dim stays 6 (not 4). The associative SKELETON EXISTS for a SINGLE `J` — Phase-1.1's "triple does not exist" verdict is REPAIRED at the level of the associative skeleton, closing both bridges `f0_real_structure_gate` left open (KO-6-restoring rebuild; Yukawa in the real structure). HONEST CAVEAT: this recovers the KNOWN Connes-Chamseddine-Marcolli skeleton constructively — the complement to the no-go, NOT new physics — and moves NO Bayes credit. Open CHO-specific bridges: step C (replace the generic Yukawa by the octonionic Jordan mass operator `L_X` and realise the full `432 = 16 (A_Weyl) x 27 (J3(O))` module; only the 8-dim colour-singlet lepton slice is built here) and Phase 1.3 (`eps0^2 = pi/432` as the spectral-action ratio `a4/a2` — `epsilon_heat_kernel` warns the spectral `pi` enters only via the Gaussian `(4 pi)^(-d/2)`, so a bare `pi` numerator is unlikely). F0 stays GEOMETRIC/open; `eps0^2 = pi/432` is not promoted.
- `compute/f0_octonionic_yukawa_gate.py` — Phase-1.2 step C: puts the SPECIFIC octonionic Jordan mass operator `L_X` (the step-B gate used a generic Yukawa) into the KO-6 triple and asks whether the axioms FORCE its texture. The faithful SM finite geometry is `H = C^8 (x) C^27` — the step-B lepton charge factor tensored with the `J3(O)` flavour `27` — and the Yukawa is `K_Yuk (x) L_X` with the charge `L<->R` coupling `K_Yuk` `gamma1`-ODD and `L_X` the UNGRADED octonionic generation matrix (`gamma = gamma1 (x) I27`, `J = J1 (x) conj`). From explicit `8x8`/`27x27`/`216x216` matrices: **(A)** `L_X` is the genuine averaging-law operator — spectrum = three singlets `{1, 0.6, 0.3}` (mult 1) and three octets `{0.8, 0.65, 0.45}` (mult 8), `27` total, self-adjoint to `0`; **(B)** the second Phase-1.1 obstruction (`L_X` chirality-even → Connes-doubling → the `6 (x) 6 -> 4` KO collapse) is DISSOLVED — because chirality sits in the charge factor, `L_X` needs NO doubling: the product Dirac `D = K_Yuk (x) L_X + K_Maj (x) M_maj` is self-adjoint, `gamma`-ODD, `J`-REAL, `KO`-dim 6 (`eps = +1`, `eps'' = -1`), with order-zero (`~9e-16`) and order-one (`~9e-16`) BOTH holding for the genuine octonionic `D`; **(C)** DECISIVE — order-one factors through the charge sector: each charge coupling tensored with a RANDOM Hermitian flavour operator still satisfies order-one (`K_Yuk (x) random ~7e-15`, `K_Maj (x) random = 0`), so the gauge algebra sees the flavour factor as pure multiplicity and ANY self-adjoint flavour operator passes. TWO-SIDED VERDICT: POSITIVE — the octonionic `L_X` lives in a consistent KO-6 triple ungraded, carrying its averaging-law masses into `D`'s spectrum (closes the second Phase-1.1 obstruction); SOBERING — the triple axioms (order-zero/one, KO-6) are NECESSARY but do NOT pin the Yukawa, so the CHO mass texture is NOT secured by the triple's existence — it must come from the spectral ACTION `Tr f(D/Lambda)` (Phase 1.3), and `epsilon_heat_kernel` already warns the spectral `pi` enters only via `(4 pi)^(-d/2)`, so Phase 1.3 is more likely to REFUTE than confirm `eps0^2 = pi/432` as `a4/a2`. F0 stays GEOMETRIC/open; moves NO Bayes credit.
- `compute/f0_spectral_action_heatkernel.py` — Phase-1.3 DECISIVE EXPERIMENT, the gold-standard make-or-break test: does `eps0^2 = pi/432` equal the spectral-action heat-kernel ratio `a4/a2` of the genuine 216-dim octonionic step-C Dirac `D`? For the finite triple the Seeley-DeWitt coefficients ARE the spectral moments `a0 = M0 = Tr(1) = 216`, `a2 = M2 = Tr(D^2) = 92.96`, `a4 = M4 = Tr(D^4) = 50.3712`. **(A)** the moments are read off the explicit Dirac spectrum; **(B)** every dimensionless `a4/a2` normalisation MISSES `pi/432 = 0.00727221` — the closest natural shape `M4/M2^2 = 0.00582895` is `0.80x` target, a clean 20% miss; **(C)** the STRUCTURAL KILL (not a numerical near-miss): the moments are EXACT rationals (`M2 = 2324/25`, `M4 = 31482/625`, residual `~1e-14`) because `Tr(D^2k)` is a rational power sum of the algebraic Dirac spectrum, so `a4/a2 = M4/M2^2 = 15741/2700488` is an exact rational and can NEVER equal the transcendental `pi/432` (`pi/432` has no small-denominator fit, residual `5e-7`); seed-independent — seed `(.8,.6,.4)` gives `M2 = 2002/25`, `M4 = 20584/625`, another `pi`-free rational; **(D)** the ONLY `pi` a Connes-Chamseddine spectral action emits is the continuum `(4 pi)^(-d/2)` — a DENOMINATOR `pi` with half-integer power, `(4 pi)^-2 = 0.00633 != pi/432` — confirming `epsilon_heat_kernel`; **(E)** the bare `pi` numerator is reproduced by the Berry half-solid-angle `(1/2)(2 pi) = pi`, a holonomy flux, so `pi/432 = (Berry pi) x (Schur 1/432)` is a flux-per-state count, a GEOMETRIC quantity not a spectral-action output. VERDICT (the roadmap KILL branch): Phase 1.3 REFUTES `eps0^2 = pi/432` as the heat-kernel `a4/a2` — the DYNAMICAL earn-path for the `+5.6` via the spectral action is CLOSED (any DERIVED promotion now needs a different mechanism), but the Berry/Schur GEOMETRIC reading is UNTOUCHED and remains the ceiling for `pi/432`, so F0 stays GEOMETRIC/open (NOT demoted below geometric, NOT promotable via this route). Moves NO Bayes credit; the scoreboard ladder (`-21.3` historical / `-3.2` EARNED floor / `+5.6` if-granted / `+36.2` target) is UNCHANGED and no frozen artifact is touched.
- `compute/f0_phase1_closeout.py` — Phase-1 CLOSEOUT: the make-or-break finite-spectral-triple gate is fully executed (1.1 obstructions -> 1.2 associative rebuild + octonionic `L_X` -> 1.3 heat-kernel refutation -> 1.4 spectrum localisation), and this artifact consolidates its TWO independent decisive results by importing the source-of-truth numbers from `f0_spectral_action_heatkernel` and `spectral_action_432`. **PREFACTOR route (Phase 1.3):** `a4/a2 = M4/M2^2 = 0.00582895` is a `pi`-free rational, bounded away from `pi/432 = 0.00727221` (gap `0.00144 > 1e-3`) — `pi/432` is NOT a spectral-action output. **RATIO route (Phase 1.4):** the `L_X` averaging-law spectrum's best single-knob `eps0` ladder MISSES the measured charged-lepton hierarchy by `1.40` decades — the spectrum forces STRUCTURE, not the absolute profile. **CONVERGENCE:** the prefactor (a transcendental constant) and the ratios (multiplicative) are INDEPENDENT tests yet BOTH localise the entire remaining F0 gap to the SAME missing object — a DYNAMICAL/VARIATIONAL action that would have to produce `pi/432` [refuted] AND select the three seed eigenvalues [open] — gold-standard criterion 1, ABSENT (`foundations/02_action.md` candidates it). FORK OUTCOME (bounded): F0 stays GEOMETRIC/open (Berry/Schur reading survives, mass STRUCTURE derived), neither `pi/432` nor the hierarchy promotable without the action; Phase 2 GATED on it. This is a CONSOLIDATION, not new physics — it closes the invariance-witness phase and moves NO Bayes credit; the scoreboard ladder and frozen registry are untouched.
- `compute/epsilon_cubic_discriminant.py`, `compute/epsilon_heat_kernel.py`, `compute/epsilon_state_count.py`, `compute/epsilon_product_space.py`, `compute/epsilon_weyl_isomorphism.py`, `compute/epsilon_spin9_embedding.py`, `compute/epsilon_rank_one_kernel.py`, `compute/epsilon_free_action.py`, `compute/epsilon_a4_two_level.py`, `compute/epsilon_channel_coefficients.py`, `compute/epsilon_mixing_coefficients.py`, `compute/epsilon_vcb_halfangle.py` — twelve triangulating experiments on `epsilon0^2 = pi/432` (F0) and its sector coefficients. They rule out the heat-kernel origin of the `pi` and the cubic-discriminant origin of the `27`, DERIVE `16 = dim OP^2 = F4/Spin(9)` as the rank-one idempotent manifold of `J3(O)`, stratify `27 = 1 + 16 + 10`, DISCHARGE the isomorphism `A_Weyl ~= T(OP^2)` (both are the unique 16-dim real Spin(9) spinor `Delta_9`), close the gauge-vs-flavour Spin(9) seam by recovering the flavour octonionic `Cl(9)` (Casimir `1,9,126`; bivectors = flavour `so(9)`; `O(16)`-conjugate), reframe R1 (rank-one kernel = primitive idempotent = pure single-generation vacuum, dual to `N_gen=3`), reframe R2 (the free action + topological term is the unique two-level-symmetric weight, up to a `theta`-irrelevant scale) and trace that two-level symmetry to the `SU(2)` closure of the `A4` flavour group (`Q8 = ` double cover of `V4 < A4`, irreducible by Burnside; `A4/V4 = Z3` the generation grading), derive the mass-sector channel ranks `(1, 3, 8)` as number-operator Fock-grade traces (the lepton `8 = Tr I_Fock = 2^3`, closing M3), derive the mixing multiplicities `(7, 3, 4, 4/7)` as Fano-line counts (all lines / lines through the vacuum / lines avoiding it) with the lepton `1/(4 pi)` identified as the transition-sphere measure (advancing M11/C1/N2/N3/N5), and derive the `|V_cb|` coefficient `1/2` as the `SU(2)` spinor half-angle (`sin(eps0/2)`, with the finite avatar `tan(pi/8) = sqrt(2)-1`).
- `compute/yukawa_bridge.py` and `YUKAWA_BRIDGE.md` — charged-flavour scaffold deriving leading NNI adjacency and the cascade relation, while isolating sector shape factors as operator targets.
- `compute/pmns_bridge.py` and `PMNS_BRIDGE.md` — PMNS scaffold deriving TBM residual symmetries and printing the broken-triality Majorana perturbation target.
- `compute/cho_bridge_operator.py` and `CHO_OPERATOR.md` — unified candidate operator embedding the epsilon trace, sector projectors, NNI shape factors, Fano phase diagnostics, and PMNS seesaw target.
- `foundations/09_yukawa_operator_theorem.md` and `compute/yukawa_operator_full.py` — Phase 3 one-operator gate. It wraps the composite CHO operator into one executable ledger, prints charged-sector NNI/Fritzsch matrices, compares strict CKM from one charged diagonalization against the separate CKM magnitude projection, and reports the PMNS `DeltaY`/`DeltaM` target. Current verdict: audit coherent, theorem OPEN; CKM one-diagonalization and PMNS dynamics remain demotion triggers.
- `foundations/10_continuum_rg.md` and `compute/rg_matching_audit.py` — Phase 4 continuum/RG gate. It separates CHO algebraic boundary terms, standard one-loop SM running, VP/threshold inputs, and target-implied matching scales. Current verdict: audit coherent, theorem OPEN; `alpha`, `sin^2(theta_W)`, `M_W`, and `Lambda` must not be promoted until the matching scale, threshold scheme, electroweak normalization, and free-energy screen are derived from CHO.
- `compute/rg_scale_derivation.py` — Item 3 "close or kill" of the electroweak matching scale (the KILL branch). It tests whether a SINGLE matching scale can carry both CHO electroweak boundaries at once. Treating `alpha_em^-1 = 128 pi/3` and `sin^2 = 1/4` as fixing both gauge couplings (`alpha_1^-1 = 96 pi/5`, `alpha_2^-1 = 32 pi/3`) at one scale, standard one-loop running reaches them at `mu ~ 12 GeV` and `mu ~ 2.2e5 GeV` — a `1.8e4` discrepancy, so the single-continuum-scale reading is FALSIFIED. Corroborating: the lone inverse-matched `sin^2 = 1/4` scale is `M_P/3^32.5` (a non-integer power of 3), and no independently-derived CHO scale (`v`, `M_W`, seesaw, `M_P`) yields `1/4` (closest miss `0.0138`). Net: the matching scale is inverse-fit, not derived; per the kill rule in `foundations/10_continuum_rg.md`, S4/S5 stay Open bridge with a sharper, falsification-grade reason. No ledger row is promoted and the scoreboard is untouched.
- `compute/neutrino_floor_resolution.py` — Item 4 resolution of the neutrino-floor falsifier (a RESOLVE-by-honest-error-budget branch, the counterpart to Item 3's kill). The headline `~4.6 sigma` deficit of `m_nu3 = 48.9 meV` below the `50.1 meV` oscillation floor divides the `1.2 meV` gap by the floor's EXPERIMENTAL error alone, assigning zero theory error to a TREE-LEVEL seesaw. Calibrating the two `O(1)`-uncertain inputs from their own sister rows (`M_R` normalization from `M_W = M_P/3^36`, `+1.2%`; `y_nu3 = 1` from the `y_t = 1` top saturation `m_t = v/sqrt2`, `+0.8%`, entering squared) gives a `~2.0%` tree-level theory error (`+/- 1.0 meV`), so the honest significance is `1.2 sigma` — consistent, not a clean kill. DECISION: N1 is DEMOTED from a precision claim to order-1 consistency; the central value is stated plainly as still sitting just below the physical floor (not hidden, not spun); the live obligation is to DERIVE the `O(1)` normalization and the genuine kill is a pinned derived normalization still below the floor. The frozen `Sigma m_nu` band (57-62 meV) already covers the minimal on-floor sum (58.7 meV), so the locked forward prediction is unaffected. No ledger row is promoted and the scoreboard is untouched.
- `compute/operator_gap_audit.py` and `OPERATOR_GAP_AUDIT.md` — explicit audit of unsolved operator, CKM, PMNS, and continuum/RG gaps.
- `compute/sector_projector_derivation.py` — sector-count diagnostic for the Fock-grade ranks; later epsilon modules derive the mass ranks `1`, `3`, and `8` as number-operator traces and identify the lepton `1/(4 pi)` as the transition-sphere measure, leaving their selection by one CHO Yukawa operator open.
- `compute/first_generation_audit.py` — confronts the `m_e` `-3.75 sigma` audit outlier: decomposes each first-generation mass error (M9-M11) into intrinsic bridge-factor error (against measured inputs) vs propagated error (from feeding predicted 2nd/3rd-gen masses into squared ratios). Shows the headline `-5.6%` on `m_e` is mostly propagation and isolates the genuine `1/(4pi)` proof obligation at ~2%.
- `compute/scale_look_elsewhere.py` — log-axis look-elsewhere for the power-of-three SCALE relations (`M_W = M_P/3^36`, `M_R = M_P/3^9`, `Lambda^(1/4) = (11/12) M_P/(sqrt(2) 3^64)`), which `look_elsewhere.py` deliberately excludes. With the same simple prefactor vocabulary a `prefactor x integer-exponent` pair covers `~88-100%` of one exponent window (mean `~93%`), so these hierarchy hits are CHEAP: the real content is the integer exponent plus an `O(1)` prefactor, not a forced number. The `Lambda` row (prefactor `11/12 / sqrt(2)`) is weakest (`100%` coverage, CHO rank 30). A base sweep (`2, 3, e, pi`) shows base 3 is not uniquely forced by the data either. This is the honest counterpart to the `12/12 simplest fitter` headline, which applies only to the dimensionless coefficients.
- `compute/mass_ratio_rg_audit.py` — per-relation renormalization-scale audit. Classifies each headline mass relation as 1-loop RG-invariant (same-sector ratios whose QCD anomalous dimensions cancel: `m_c/m_t`, `m_s/m_b`, `m_mu/m_tau`, and the products `m_s m_t/(m_b m_c)`, `m_mu m_b/(m_tau m_s)`) vs scale-dependent. Only `m_b/m_tau = 7/3` (M5) is scale-dependent (lone quark/lepton ratio); it holds at `mu ~ m_b` (0.8%) and drifts to `-70%` at the GUT scale. Corrects the previously incorrect claim in `rg_running.py` that all lepton-involving inter-sector ratios are RG-invariant.
- `FLAVOUR_DERIVATION.md` — companion memo explaining the scaffold, its inputs, and what remains to be proven by a full CHO Yukawa operator.
- `compute/jordan_eigenvalue_generations.py` (Lever A) — spectral route to "three": the Freudenthal cubic of J3(O) has degree 3 with three real roots (verified on 4000 random Hermitian samples) and three orthogonal primitive idempotents resolving the identity; "three" is the rank of the algebra, immune to the triality mirror obstruction. Honest negative on physical content (a diagonal element returns only its seeded spectrum).
- `compute/ko_dimension_chirality.py` (Lever B) — builds Cl(0,7) from left-multiplications, the chirality grading and the complex-conjugation real structure, and computes (epsilon, epsilon'') = (+1, -1) -> KO-dimension 6, the Connes value for chirality without fermion doubling. The structural cure for the mirror pair that downgrades G1.
- `compute/spectral_action_432.py` — the decisive cross-generation inverse-spectral test (Option B; sequel to `spectral_action.py`'s one-generation negative). Builds the algebra-internal Yukawa on the generation factor `J3(O)` as Jordan left-multiplication `L_X` (27x27, from the structure tensor) and runs the same knobs-in vs constants-out accounting. RESULT (strict improvement on the one-generation negative, still PARTIAL): (1) the spectrum of `L_X` obeys a VERIFIED parameter-free AVERAGING LAW — `{a,b,c}` plus `{(a+b)/2,(b+c)/2,(c+a)/2}` each x8 (residual ~1e-7, multiplicity `[1,1,1,8,8,8]`), i.e. every mixing level is the mean of two generation levels: `constants_out = 3` forced relations; (2) the existing single spurion `eps0^2 = pi/432` cuts the generation knobs from 3 to 1; (3) but no one-knob ladder in `eps0` reproduces the measured lepton hierarchy (best geometric ladder misses the lightest state by ~1.4 decades). NET: the open Yukawa SPECTRUM is localised to ONE scalar seed function; the mixing law is derived. Closing it needs a dynamical selection principle — the lone high-risk open item.
- `compute/epsilon_generation_ladder.py` — exponent spectroscopy on that lone seed: write each charged-generation mass as a power of the FORCED base `eps0 = sqrt(pi/432)` (`p_n = log_{eps0}(m_n/m_heaviest)`) and test simple integer-exponent laws with a look-elsewhere count and a cross-sector universality test. RESULT: the scheme-clean LEPTON ladder is far better fit by TRIANGULAR exponents `(0,1,3)` (worst miss 0.33 dec, i.e. log-mass quadratic in generation index) than by the geometric `(0,1,2)` ladder (1.40 dec) `spectral_action_432.py` tested, and that triangular hit is the ONLY one of 28 integer triples that fits within 0.4 dec (4% look-elsewhere coverage, so NOT cheap). The up-type middle ratio is `m_c/m_t = eps0^2` to ~0.2% (exponent 2.00). BUT no single law is universal (up best `(0,2,5)`, down best `(0,2,3)`; quark sectors carry MS-bar/scale caveats per `mass_ratio_rg_audit.py`), so this is a derived-LOOKING pattern, not a theorem. It sharpens the open seed from "one free profile" to "a (possibly sector-dependent) log-mass CURVATURE the dynamics must output".
- `compute/spurion_perturbation.py` — turns two of the follow-up ideas into checked algebra (both verified from the J3(O) structure tensor, no fitting). FACT 1 (rank-one bottleneck): the repo's single spurion `T_break = theta|tau><tau|` is rank one, and a rank-one perturbation of a degenerate level lifts EXACTLY ONE eigenvalue at first order (numerically 1.0e-3 vs 4e-19) — so a three-tier hierarchy cannot come from one first-order insertion; the tiers must appear at cumulative orders `eps^1, eps^2, ...`, which is what makes triangular exponents natural. FACT 2 (multiplicative mixing): the canonical Jordan QUADRATIC operator `U_X Y = 2 X o (X o Y) - (X o X) o Y` has spectrum `{a^2,b^2,c^2}` and `{ab,bc,ca}` (each x8) for `X=diag(a,b,c)` — residual EXACTLY 0, multiplicity `[1,1,1,8,8,8]` — i.e. mixing levels are GEOMETRIC means (products), so under `U_X` log-mass is ADDITIVE in generation exponents (contrast `L_X`'s arithmetic means). Together these say a power-law ladder requires the quadratic operator plus cumulative insertions; the minimal nilpotent chain `c_n=(0,1,2)` then gives triangular diagonal exponents `(0,1,3)` matching leptons to 0.33 dec. OPEN (not faked): a dynamical proof that the chain is exactly `c_n=n` and its sector dependence.
- `compute/generation_cascade.py` — attacks that lone seed with the framework's own commitment: the three generations are the three roots of the Freudenthal characteristic cubic of a `J3(O)` Yukawa element, whose coefficients are exactly the three F4 invariants `(T1, T2, N3)` = (trace, quadratic, cubic norm). DERIVED (verified, no fitting): THEOREM 1 the heaviest generation equals the trace (`|m1/T1-1| ~ 1e-3` at strong hierarchy); THEOREM 2 (cubic-norm SEESAW) the light-pair product is EXACTLY the cubic norm over the heaviest mass, `m2*m3 = |N3|/m1` (Vieta residual `5e-15`), so the lightest fermion is pushed down by the Freudenthal cubic norm; COROLLARY in the seesaw regime `2*ord(T2) <= ord(N3)` the three generation exponents are `(0, ord T2, ord N3 - ord T2)`. NET REDUCTION: the open seed shrinks from THREE continuous masses per sector to TWO integer suppression orders `(q,Q)=(ord T2, ord N3)`; the scheme-clean leptons read the clean `(1,4)` = triangular `(0,1,3)`. OPEN (not faked): `(q,Q)` are NOT universal across sectors (up `~(2,7)`, down `~(2,4)`) and not yet derived from a dynamical principle — that discrete, two-integer, sector-dependent question is the residue.
- `compute/cascade_universality.py` — resolves the second-generation half of that residue: the cascade `(q,Q)` sector-dependence is NOT new freedom but the flavour data the framework already derives. Writing the spectrum as a Freudenthal seesaw gives the UNIVERSAL skeleton `(0,2,4)` (`m2/m1 = eps0^2`, `m3/m1 = eps0^4`); every sector deviation is a `log_{eps0}()` image of two prefactors. Dividing the measured middle exponent `ord(T2)` by the INDEPENDENTLY DERIVED Georgi-Jarlskog integers `c2 = {up:1, down:3, lepton:8}` (the `8/3 = dim(O)/N_color` relation, not fitted — they match the masses to ~2%) yields a UNIVERSAL `a_reduced = 2.00 +/- 0.01` across all three sectors, and the measured `(q,Q)` are reproduced to `<1%` from `(2,4)` + prefactors. So the second-generation suppression is universally `eps0^2`; the apparent `q` scatter is the derived GJ factors. OPEN (not faked): the LIGHTEST generation does NOT become universal — its reduced exponent is `4 + log_{eps0}(c3)` with the first-generation prefactor `c3` sector-dependent (lepton `c3 = 0.081 ~ 1/(4 pi)` identified, but up `~1/4` and down `~2.2` remain the existing open first-generation anomalies). Net: the cascade's open `(q,Q)` residue collapses to the already-open first-generation O(1) prefactors.
- `compute/ladder_charges.py` (Lever C) — reproduces the Furey/Dubois-Violette result numerically: the C x O number operator gives one-generation electric charges {0, 1/3, 2/3, 1} with colour multiplicities (1,3,3,1), the stringent hypercharge filter, as an output of the algebra.
- `compute/weak_isospin_hypercharge.py` (Lever D) — derives weak SU(2) from the quaternions H < O (T_a=(i/2)L_(e_a), [T_a,T_b]=i eps T_c, Casimir 3/4, T_3=+-1/2 doublet), shows the so(4)=su(2)_L (+) su(2)_R left/right split, the direct product SU(2)_w x SU(3)_c with chirality preserved, and via Gell-Mann-Nishijima Y=2(Q-T_3) reproduces the entire SM hypercharge spectrum {1/3, 4/3, -2/3, -1, -2, 0} of one 16-fermion generation. One named seam left open: the KO-dim-6 chiral projection that makes only LH fields doublets (Lever B <-> Lever D linkage).
- `compute/bayesian_evidence.py` — model-comparison Bayes factor CHO vs an O(1)-numerology null on the independent set, correlation- and parameter-penalised. Charges the full Occam price for every chosen prefactor and credits nothing for derived ones; default credits only the numerically-closed prefactors (today's conservative floor).
- `compute/scoreboard.py` — reads the Bayes factor as one number across a credit ladder. The `eps0` program moved `ln B` from `-21` (only `8/3` closed) to `-3` (today's closed theorems) to `+6` (crediting the geometric `pi/432`); the sign-flip is pinned to whether `pi/432` is geometrically forced. The credited sets are the same status tags audited in `model_complexity.py`, so the scoreboard cannot be moved by hand.
- `compute/prediction_registry.py` — Phase 6 locked prediction registry: positive quantitative predictions (`Sigma m_nu`, `theta23`, `m_betabeta`) are separated from bridge sensitivities (`m_nu3` floor tension, `kappa_lambda`), with formula, frozen inputs, channel, kill condition, date, per-entry SHA-256 digest, and a locked manifest digest. Any silent retune now fails the audit.
- `compute/jordan_standalone_theorems.py` and `PAPER_JORDAN_THEOREMS.md` (Item 5, publication) — collects the genuinely-novel-in-assembly mathematics into THREE theorems decoupled from every physical claim, each machine-verified: (A) the Jordan-frame `S3` acts by INNER automorphisms of the connected `F4 = Aut(J3O)` (drift of the three F4 invariants under all 6 frame perms `~1e-14`), so the three idempotents are F4-congruent `OP^2 = F4/Spin(9)` points (isotropy `[36,36,36]`, tangent `[16,16,16] = Delta_9`, real-type commutant `1`) — categorically unlike the OUTER `Spin(8)` triality that permutes the inequivalent `8v,8s,8c`, so a Distler-Garibaldi mirror obstruction cannot even be posed; (B) Schur forces the flat weights `1/16` (Spin(9) on `Delta_9`) and `1/27` (E6 on the `27`, where F4 alone leaves it reducible `1+26` with trace-weight `1/3`), product `1/432`; (C) Vieta on the Freudenthal cubic gives the exact seesaw `m2*m3 = |N3|/m1` (residual `5e-15`). Every ingredient is CLASSICAL (Albert algebra/frames, F4 connectedness, `OP^2`, real Spin(9) spinor, E6 reduced structure group, Schur, Vieta) and cited; the contribution is the decoupled assembly + three observations. Registered `diagnostic`: it does NOT close G1/F0/A3 and must not be cited as physical evidence.
- `compute/theta23_octant_prediction.py` (Item 7, the single sharpest falsifiable claim) — stakes ONE sharp, pre-registered bet on an UNMEASURED quantity: `sin^2(theta23) = 4/7 = 0.5714` (`theta23 = 49.1 deg`, UPPER octant). It is selected as the sharpest of all CHO predictions because it is the ONLY mixing number that is (i) an exact rational AND `eps0`-INDEPENDENT — the module verifies `d sin^2 theta_23 / d eps0 = 0` exactly under an `eps0 +/- 20%` scan while the control `sin^2 theta_13 = 3 eps0^2` moves — so it stands clear of the open `pi/432` seam that hinges the Bayes factor; (ii) a clean PASS at experimental precision in the Item-6 per-row audit (exp pull `-0.02`); and (iii) a verdict on a binary that is currently UNRESOLVED (T2K/NOvA tension), so a genuine forward bet. The octant is the FANO DISCRIMINATOR: the lower-octant mirror is exactly `cos^2 = 3/7`, and the same partition that fixes the value — `4` Fano lines avoiding the vacuum `e7` vs `3` through it — fixes the octant (`4 > 3` IS "upper"), so the octant measurement directly tests which Fano class controls `theta23`. KILL: a stable lower-octant resolution (`sin^2 theta23 < 1/2`, the `3/7` side) falsifies the Fano assignment; an upper-octant value pinned far from `4/7` (beyond the few-percent N5 bridge error) falsifies the precise value. Decisive reach: DUNE, Hyper-Kamiokande. Registered `future_test` on ledger N5; the module cross-checks the frozen registry payload (Q2) read-only and asserts only stable arithmetic — it promotes NO ledger row and leaves the Bayes factor untouched.
- `compute/gravity_curvature.py` (M-GRAV) — the first non-placeholder gravity brick. Builds the emergent metric perturbation as the Gram pullback of the octonionic associator (transport defect `M_(a,b)(x)=[x,a,b]`) and verifies it is symmetric, positive-semidefinite, `G2`-covariant (rank-2 tensor, exact on all 1344 finite automorphisms), with scalar density `tr g = 16|a^b|^2`, rank 4 and transverse to its source bivector (graviton-like), and null space = the associative quaternionic subalgebra (Artin geometric). Includes the standard `C x H = M_2(C)` Minkowski + `SL(2,C)` Lorentz arena. Honest residual: the internal `Im(O)` [`G2 < SO(7)`] metric is NOT yet reduced to a 4-d spacetime `SO(3,1)` metric, the two CHO halves are not joined, and there is no dynamics (Conjecture 6.1, ledger GR1).
- `foundations/11_gravity_gate.md` and `compute/gravity_gate_audit.py` — Phase 5 gravity gate. It verifies the flat `C x H` Lorentzian arena and the internal kinematic metric, then shows why the gravity line does not close: the finite `G2` witness has no nontrivial invariant four-plane (`commutant dim = 1`; rank-four projectors average to `(4/7)I`), the associator metric is positive-semidefinite rather than Lorentzian, and no Einstein/Newton dynamics emerge. Current verdict: audit coherent, gravity OUT OF SCOPE for the present framework.
- `METHODOLOGY_LIMITS.md` — caveat document for few-input parameter language, postdictions, dependent audit rows, continuum/RG gaps, dimensional-counting risks, and null-test scales.
